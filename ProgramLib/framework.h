#pragma once
#include <vector>
#include <string>

using namespace std;

using StringBuffer = vector< string>;
#define WIN32_LEAN_AND_MEAN             // Exclude rarely-used stuff from Windows headers
